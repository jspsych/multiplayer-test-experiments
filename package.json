{
  "name": "multiplayer-test-experiments",
  "private": true,
  "description": "Reference-game replication builds on jspsych-multiplayer",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "jzip": "node scripts/make-jzip.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
